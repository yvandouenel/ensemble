uuid: e773b2d1-db02-4f31-b883-0d08a581dacb
langcode: fr
status: true
dependencies:
  config:
    - field.storage.media.field_tag_media
    - media.type.image
    - taxonomy.vocabulary.tag_media
id: media.image.field_tag_media
field_name: field_tag_media
entity_type: media
bundle: image
label: Tag
description: "Les \"tags\" sont des mots clés qui permettent de retrouver les images.Nne placer ici que des mots clés génériques. Si plusieurs mots clés sont nécessaires, les séparer par une virgule (,).\r\nExemple: imaginons que vous vouliez taguer la photo du portrait de François Ruffin : ne pas utiliser le terme Ruffin qui devra se trouver dans le titre de l'image mais plutôt : portrait, personnalité"
required: true
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:taxonomy_term'
  handler_settings:
    target_bundles:
      tag_media: tag_media
    sort:
      field: name
      direction: asc
    auto_create: false
    auto_create_bundle: ''
field_type: entity_reference
